package database

object TEXT {
    object LIMIT {
        const val DEFAULT = 255     // TEXT.LIMIT.DEFAULT
        const val DOUBLE = 510      // TEXT.LIMIT.DOUBLE
    }
}

object DECIMAL {
    object PRECISION {
        const val DEFAULT = 10      // DECIMAL.PRECISION.DEFAULT
    }
    object SCALE {
        const val DEFAULT = 2       // DECIMAL.SCALE.DEFAULT
    }
}